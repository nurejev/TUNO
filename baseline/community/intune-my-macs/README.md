# intune-my-macs — the macOS community baseline

Cut verbatim from https://github.com/microsoft/intune-my-macs at commit `0fe22b65ae96ca0a3af85b7994a83b28d764d592` (release 2026-08-07, published 2026-08-07) by Microsoft. 21 policies, names and descriptions the author's own.

Schema 2 · catalog `imm` · read into this repository on 2026-09-03. Three dates, never one string: `release` is the author's own version, `sourceDate` is when THEIR cut was published, `exported` is when it was read here. Every body is the canonical body and carries the SHA-256 the loader recomputes.

`catalog.json` **is the catalog the app reads** — TUNO fetches `baseline/community/intune-my-macs/catalog.json` from its own origin when 🍎 macOS baseline opens. Written by the app: on the baseline tenant, 🍏 Upstream → fetch or load the repository → ⬇ Community catalog folder (zip), unzipped at the repository root. Never edited by hand.

| Section | Policies |
| --- | --- |
| Settings catalog policies | 19 |
| Compliance policies | 1 |
| Device configuration profiles | 1 |
